// Steps 7–9: Organic road drawing & pruning (skeleton)
export function drawOrganicRoad(ctx, a, b, randomize = 0.15) {
  // TODO: implement jittered polyline or cubic spline between a and b
}

export function pruneRoads(roads) {
  // TODO: remove redundant or ugly roads based on angles/degree/betweenness
  return roads;
}
